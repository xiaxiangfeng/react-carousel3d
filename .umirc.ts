import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'react-carousel3d',
  favicon: '/image/3d.png',
  logo: '/image/3d.png',
  outputPath: 'docs-dist',
  // more config: https://d.umijs.org/config
});
